import { Outfit, DM_Sans } from 'next/font/google'

export const OutfitFontFamily = Outfit({
    display: "swap",
    subsets: ["latin"],
});

export const DM_SansFontFamily = DM_Sans({
    display: "swap",
    subsets: ["latin"],
});