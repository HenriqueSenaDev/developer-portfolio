import { Outfit, DM_Sans, Plus_Jakarta_Sans } from "next/font/google";

export const OutfitFontFamily = Outfit({
  display: "swap",
  subsets: ["latin"],
});

export const DMSansFontFamily = DM_Sans({
  display: "swap",
  subsets: ["latin"],
});

export const PlusJakartaSansFontFamily = Plus_Jakarta_Sans({
  display: "swap",
  subsets: ["latin"],
});
